@model List<UserViewModel>

<div class="text-center">
    <h3>Список пользователей</h3>
</div>
<div class="row">
    <div class="col-md-2">
        @await Html.PartialAsync("AdminLeftMenu")
    </div>

    @{ int counter = 1;

        <div class="col-md">
            <table class="table">
                <thread>
                    <tr>
                        <th>№</th>
                        <th>Пользователь</th>
                        <th>Почта</th>
                        <th>Телефон</th>
                    </tr>
                </thread>
                <tbody>

                    @foreach (var user in Model)
                    {
                    <tr>
                        <td>@counter</td>
                        <td>@user.UserName</td>
                        <td>@user.Email</td>
                        <td>@user.PhoneNumber</td>
                        <td><a class="btn btn-outline-dark" asp-area="Admin" asp-controller="User" asp-action="Detail" asp-route-userName="@user.UserName">Подробнее</a></td>
                    </tr>

                        counter += 1;
                    }

                </tbody>
            </table>
            <div class="col-md text-right">
                <a class="btn btn-outline-dark" asp-area="Admin" asp-controller="User" asp-action="Add">Добавить нового пользователя</a>
            </div>
        </div>
    }
</div>