@model UserAccount

<div class="text-center">
    <h3>Логин: @Model.Login</h3>
</div>
<div class="container">
    <div class="row">
        <div class="col-md-2">
            @await Html.PartialAsync("AdminLeftMenu")
        </div>
        <div class="col-md">
            <table class="table">
                <thread>
                    <tr>
                        <th>Подробная информация</th>
                    </tr>
                </thread>
                <tbody>
                    <tr>
                        <td>Id</td>
                        <td>@Model.Id</td>
                    </tr>
                    <tr>
                        <td>Фамилия</td>
                        <td>@Model.LastName</td>
                    </tr>
                    <tr>
                        <td>Имя</td>
                        <td>@Model.FirstName</td>
                    </tr>
                    <tr>
                        <td>Телефон</td>
                        <td>@Model.Phone</td>
                    </tr>
                </tbody>
            </table>
            <div class="col-md text-right">
                <button class="btn btn-outline-dark" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Действия
                </button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                    <a class="dropdown-item" asp-area="Admin" asp-controller="User" asp-action="ChangePassword" asp-route-login="@Model.Login">Смена пароля</a>
                    <a class="dropdown-item" asp-area="Admin" asp-controller="User" asp-action="Edit" asp-route-userId="@Model.Id">Редактирование данных</a>
                    <a class="dropdown-item" asp-area="Admin" asp-controller="User" asp-action="EditRights" asp-route-userId="@Model.Id">Права доступа</a>
                    <a class="dropdown-item" asp-area="Admin" asp-controller="User" asp-action="Remove" asp-route-userId="@Model.Id">Удалить пользователя</a>
                </div>
            </div>
        </div>
    </div>
</div>